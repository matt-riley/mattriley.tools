name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    permissions:
      contents: read
      packages: read
    uses: matt-riley/matt-riley-ci/.github/workflows/node-ci.yml@7f4f0d523d1a7f824fbb101f180bfcb463751b41 # main
    with:
      package-manager: pnpm
      run-lint: true
      run-test: true
      run-build: true

  deploy:
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: ./.github/workflows/request-infra-deploy.yml
    with:
      app-name: mattriley-tools
    secrets:
      PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
